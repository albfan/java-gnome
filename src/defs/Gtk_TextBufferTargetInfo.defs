(define-enum TextBufferTargetInfo
  (in-module "Gtk")
  (c-name "GtkTextBufferTargetInfo")
  (values
    '("buffer_contents" "GTK_TEXT_BUFFER_TARGET_INFO_BUFFER_CONTENTS")
    '("rich_text" "GTK_TEXT_BUFFER_TARGET_INFO_RICH_TEXT")
    '("text" "GTK_TEXT_BUFFER_TARGET_INFO_TEXT")
  )
)

